using System.Windows;

namespace ClaudeStats.Views;

/// <summary>
/// Settings window (launch-at-startup preference).
/// </summary>
public partial class SettingsView : Window
{
    /// <summary>Initializes the settings window.</summary>
    public SettingsView()
    {
        InitializeComponent();
    }
}
