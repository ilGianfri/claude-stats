<Window x:Class="ClaudeStats.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ClaudeStats — Settings"
        Height="200" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#FF1B1D22">
    <StackPanel Margin="24">
        <TextBlock Text="SETTINGS" Foreground="#99FFFFFF" FontSize="12" FontWeight="SemiBold" />
        <CheckBox Content="Launch ClaudeStats at Windows startup"
                  IsChecked="{Binding LaunchAtStartup}"
                  Foreground="White" Margin="0,18,0,0" />
        <TextBlock Text="Usage refreshes automatically about once a minute."
                   Foreground="#80FFFFFF" FontSize="11" Margin="0,18,0,0" TextWrapping="Wrap" />
    </StackPanel>
</Window>
